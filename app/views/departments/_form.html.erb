<%= form_for @department do |f| %>
    <%= f.label :ch_name, "中文名稱" %>
    <%= f.text_field :ch_name %>
	<br>
	<%= f.label :eng_name, "英文名稱(縮寫即可)" %>
    <%= f.text_field :eng_name %>
	<br>
    <%= f.submit %>
<% end %>