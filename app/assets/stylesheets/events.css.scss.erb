@import "application";
@import "events_component";

/*************
	layout
*************/
.layout-content{
	background-color: rgba(226, 226, 226, 0.7);
}

/*************
	module
**************/

/*------------------------
	banner
------------------------*/

.events-banner{
	@extend .container;
}

.banner__inner{
	@extend .item;
	width: auto;
	height: 300px;
}

.banner__image{
	@extend .center-block;
	width: auto;
	height: 95%;
}

.banner__caption{
	@extend .carousel-caption;
	font-size: 24px;
}

// redefine the banner color
.carousel-control.left{
	left: 5%;
} 

.carousel-control.right {
	right: 5%;
}

.carousel-control.left, .carousel-control.right {
    background-image: none;
	background-color: #a1a3a4;
	width: 5%;
	height: 20%;
	position: absolute;
	top: 40%;
}

/*------------------------
	form
------------------------*/

.form-wrapper{
	margin: 40px auto;
    padding: 30px 3px;
    height: 100px;
	background-color: rgba(100, 100, 100, 0.7);
}

@media screen and (max-width: 768px){ 
	.form-wrapper{
		height: 130px;
	}
}

.form-button-wrapper{
	@extend  .col-xs-6, .col-sm-2, .col-md-2;
	padding: 3px auto;
}

.form-text-wrapper{
	@extend .col-xs-12, .col-sm-6, .col-md-8;
	padding: 3px auto;
}

/*------------------------
	event-block group 
	attributes
------------------------*/

.events-block{
	@extend .container;
	@extend .center-block;
	margin: 20px auto;
}

#event-block1{
	background-image: url(<%=asset_path("events/bg-geometry.png")%>);
}

#event-block2{
	background-image: url(<%=asset_path("events/bg_symphony.png")%>);
}

.events--theme{
	position: absolute;
	left: 0px;
	width: 100%;
	//z-index: -1;
}


.theme-events--swirl{
	@extend .events--theme;
	background-image: url(<%=asset_path("events/bg_swirl.png")%>);
	//linear-gradient(to bottom, rgba(255,255,255,0) 0%, rgba(255,255,255,1) 70%);
	//background: #66ccff;
	//background: #ffcc99;
}
.theme-events--pinkrice{
	@extend .events--theme;
	background-image: url(<%=asset_path("events/bg_pinkRice.png")%>);
	//linear-gradient(to bottom, rgba(255,255,255,0) 0%, rgba(255,255,255,1) 70%);
	//background: #66ccff;
}


/*------------------------
	individual 
	event-block 
	attributes
------------------------*/
.event-block-wrapper{
	@extend .col-md-3, .col-sm-6, .col-xs-12;
	margin: 10px 0px;
	padding: 0px;
}

.event-block{
	background: rgba(255, 255, 255, 0.7);
	height: 200px;
	margin: 1px 3px;
	padding: 20px 10px;
	font-size: 16px;
}

.event-block__theme--today{
	//border:2px solid #f8d4d4;
	//background: rgba(255, 249, 171, 0.4);
	background: rgba(255, 255, 255, 0.7);
	border:0px solid #f8d4d4;
}

.event-block__theme--past{
	//border:2px solid #d1d1e0;
	background: rgba(209, 209, 224, 0.4);
	border:0px solid #d1d1e0;
}

.event-block__theme--future{
	//border:2px solid #c1eaff;
	background: rgba(193, 234, 255, 0.4);
	border:0px solid #c1eaff;
}

.event-block__top{
	padding: 3px 3px;
}
.event-block__bottom{
	border: 2px 0 0 0 solid #c1eaff;
}

.flag {  /* origin from css-tricks.com, via Zoe Rooney */
	width: 60px; 
	height: 36px; 
	padding: 3px 5px;
} 

.flag:after { /* origin from css-tricks.com, via Zoe Rooney */
	content: ""; 
	position: absolute; 
	left: 0; bottom: 0; width: 0; height: 0; 
	border-bottom: 10px solid white; 
	border-left: 30px solid transparent; 
	border-right: 30px solid transparent; 
}
.event-date{
	@extend .text-right;
	//@extend .flag;
	position: absolute;
	pagging: 2px 2px;
	height: 15%;
	top: 0px;
	bottom: auto;
	right: 5%;
	font-size: 16px;
	font-weight: bold;
}
.event-date__theme--today{
	color: #ff3838;
	//background: #f8d4d4;
}
.event-date__theme--past {
	color: gray;
	//background: #d1d1e0;
}
.event-date__theme--future{
	color: #5BA1DC;
	//background: #ccf1ff; 
}
.event-type{
	//@extend .text-right;
	width: 100%;
	height: 100%;
	position: absolute;
	top: 0;
	left: 0;
	font-size: 16px
}
.wrapper{
	vertical-align: middle;
}

.event-title{
	display: inline-block;
}
.event-location{
	//@extend .pull-right;
	position: absolute;
	display: overflow;
	bottom: 10px;
	right: 20px;
}
.event-image{
	@extend .img-responsive;
	@extend .img-rounded;
	height: 120px;
	width: 250px;
}
